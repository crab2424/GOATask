# GOATask 修正・機能追加設計

## 決定事項

- フォルダ／プロジェクト削除時の所属先は、削除確認時に選択できるようにする。
  - 親へ移動（ルートの場合はルート）
  - 未分類へ移動
- メモのフォルダ一括保存は `.md` / `.txt` / `.zip` を選択可能にする。
  - コンテキストメニュー内のサブメニュー（PCはホバー、タッチ端末はタップ）で表示する。
- 共有リンクの有効期限は7日を標準値とする。
- 共有ファイルは削除操作時に即時削除する。共有リンクの期限切れはリンクを無効化し、元ファイルは別途削除する。
- Oracleの容量・インスタンス仕様の確定は後回しにする。

## 2026-07-20 セッション終了時点

### 実装済み

- ナビゲーションのルート／空白部分にコンテキストメニューを追加。
  - タスク画面：タスク作成、プロジェクト作成
  - メモ画面：メモ作成、フォルダ作成
- タスク編集中の枠外クリックによるキャンセル。
- 単語テストの回答単位保存。モード切替で回答済み結果が失われない。
- 単語テスト中にモードを切り替える際の独自確認ダイアログ。
- フォルダ／プロジェクト削除時の移動先選択。
  - 親へ移動
  - 未分類へ移動
- メモフォルダの一括保存。
  - `.md`、`.txt`、`.zip`
  - コンテキストメニュー内のサブメニュー。PCはホバー、タッチ端末はタップ。
- メモの検索・置換。
  - `Ctrl/Cmd+F` 検索、`Ctrl/Cmd+H` 置換パネル
  - 次へ／前へ、現在箇所の置換、すべて置換
- メモフォルダの一括保存で、直下のみ／子フォルダを含む範囲を選択可能。
- 設計メモをGit管理対象として保存。

### コミット

- `c82a54f feat: improve navigation and study persistence`
- `f87c494 feat: confirm leaving active study session`

### Oracle確認状況

- OCIバケットはまだ作成されていない。
- したがって、Object Storageの使用量・残容量・バケット単位の設定は未確認。
- インスタンス情報は確認済み。
  - シェイプ：`VM.Standard.E2.1.Micro`
  - OCPU：1
  - メモリ：1GB
  - ネットワーク帯域：0.48Gbps
  - ブートボリューム：47GB
  - リージョン：`ap-tokyo-1`
- コスト分析画面では、2026年7月1日〜20日の累積コストが0円。
- 次回は、Object Storageでバケットを作成する前に、作成先コンパートメント、ストレージ層、リージョン、公開／非公開設定を決める。

### 残タスク

優先順：

1. ファイル共有のDBモデル・API・UI。
2. OCI Object Storageバケット作成と接続設定。
3. アップロード制限、ユーザー容量制限、残容量表示。
4. 非公開バケット＋期限付き署名URLによる共有リンク。
5. 共有リンク標準有効期限7日、ファイル削除時の即時削除。
6. 期限切れ共有リンクの無効化処理と、Object Storage上の不要オブジェクト削除。
7. ファイルの拡張子・MIME・内容シグネチャ検証、ファイル名サニタイズ。
8. ファイル共有のエラー処理・アクセス権・容量超過時のUI。

### 次セッションの開始手順

1. `git status` と直近コミットを確認。
2. このファイルの「残タスク」を確認。
3. 検索・置換機能を実装するか、ユーザーが作成したOCIバケット情報を確認してファイル共有の設計に進む。
4. OCIバケットを利用する場合は、バケット名、コンパートメント、リージョン、公開設定、Object Storage namespaceを確認する。

## 実装方針

1. ナビゲーションのルート／空白部分にコンテキストメニューを追加する。タスク画面ではタスク・プロジェクト作成、メモ画面ではメモ・フォルダ作成を表示する。
2. タスク編集カード外のpointerdownで編集をキャンセルする。フォーム内部、ダイアログ、コンテキストメニューは除外する。
3. 単語テストは回答ごとに統計APIへ保存し、モード切替・更新・タブ終了で未保存にならないようにする。中断時の一括保存との二重保存を避ける。
4. 一括保存は、現在フォルダのみ／子フォルダを含む全体を選べる設計にする。個別メモの既存エクスポート形式を再利用する。
5. ファイル共有は非公開Object Storage＋期限付き署名URLを基本とする。DBにはファイルメタデータと共有リンクの期限・失効状態を保存する。容量上限と1ファイル上限はOracle仕様確認後に確定する。

## 削除挙動の注意

現状バックエンドは、子フォルダ／子プロジェクトを親へ繰り上げ、直下メモ／タスクをNULL（未分類）にしている。UIの説明とは一致していないため、選択した所属先をAPIで受け取る設計に変更する。

## Oracle確認箇所

OCIコンソールでは、対象インスタンスの「詳細」だけでなく、次を確認する。

- インスタンス：シェイプ、OCPU、メモリ、ブート・ボリューム、リージョン、可用性ドメイン
- ストレージ：Object Storageのバケット、ストレージ層、使用量、残容量
- テナンシー：ガバナンスと管理 > 制限、割当て、使用量
- 課金：コスト分析、無料枠／Always Freeの使用状況

画像にあるリージョン `ap-tokyo-1`、可用性ドメイン `AD-1`、Canonical Ubuntu 24.04、稼働中であることは確認できるが、OCPU・メモリ・ブートボリューム容量・Object Storage使用量は画像だけでは確定できない。
